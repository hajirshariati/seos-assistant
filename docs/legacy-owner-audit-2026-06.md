# Legacy-Owner Audit — 2026-06-29

> Generated by the `legacy-owner-audit` workflow (26 agents, adversarial-verified). Companion to `docs/legacy-removal-plan.md`. **This is a parked plan: nothing is deleted yet.**

> **ADDENDUM 2026-07-09 (Phase A landed — read this before acting on the audit below).**
> The audit's prescribed de-risking steps are now IN CODE; the TO-DOs below are historical record, not open work:
> - **STEP 2 `[legacy-cov]` logging: DONE.** Entry logs exist on the recovery hops, auto-broaden, repeated-clarifier escape, the legacy dispatch cascade + all four dispatchers, and the emit-finalize legacy branches (hard-cap / pitch-wipe / policy-contract-wipe). Grep `[legacy-cov]` in PRD logs for the soak counts.
> - **Recovery-hop GUARDs: DONE.** `denial-recovery` and `recovery-condition-search` are registered as the `"recovery"` owner in `OWNER_WORKFLOWS`, set `answerOwner="recovery"` when they rewrite, and hard-skip `productDisplayPolicy === "suppress"` turns via `recoveryHopAllowed(plan)` — the exact guard prescribed below. ("recovery-denial-search" in this audit is the same code region as denial-recovery.)
> - **support-handoff hard mode: ATTRIBUTED.** The hard takeover sets `answerOwner/cardOwner = "support-handoff"`, intent-driven reasons are exempted via `INTENT_DRIVEN_HANDOFF_REASONS`, and `handoff_on_catalog_browse` has a runtime record site.
> - **Flag defaults (for any future reader/AI): `LLM_OWNS_ALL_TURNS` defaults ON** (`LLM_OWNS_ALL_TURNS=false` is the kill switch) — the LLM-owns path IS production; `PRODUCT_TURN_ENGINE_ENABLED` defaults OFF, which is why the recovery hops (gated `!productAuthorityModeEnabled()`) are LIVE. Any analysis claiming the LLM-owns path is "dead on PRD" is wrong.
> - **Line numbers below have drifted** (the file has grown): the LLM-owns short-circuit at "chat.jsx:5898" is now ~6350+, the recovery hops at "2246/2286" are now ~2280/2330. Grep for the log tags, not the line numbers.

**Mapped paths:** 180 | **Actionable (REMOVE/DEMOTE) claims:** 19 | **Confirmed safe removals:** 0

## Headline

All 19 adversarial verdicts REFUTED their REMOVE/DEMOTE claims, so under your rules NOTHING qualifies for safeToRemove today: there is no path that is both REMOVE-upheld and turnPlanCovers=true. The legacy owners split cleanly into two groups — (A) flag-gated rollback paths that are statically dead on PRD (gated !llmOwnsTurnActive(), behind the LLM_OWNS_ALL_TURNS short-circuit at chat.jsx:5898) and must be KEPT until the kill switch is retired, and (B) a handful of LIVE-on-PRD post-processors that mutate final text/cards and ignore TurnPlan's productDisplayPolicy — these get a one-line ctx.turnPlan GUARD. The 100%-safe move is: add coverage logging + guards now, delete nothing until the documented 2-week clean-QA window closes and the kill switch is removed.

## Safe to remove NOW

**None.** All 19 REMOVE/DEMOTE claims were refuted by adversarial verification. Per the 100%-safe rule, nothing qualifies today.

## GUARD — 12 live paths needing a one-line TurnPlan check

### `denial-recovery` — `chat.jsx`

**What:** In-loop post-processor that force-searches and rewrites the model's text from 'we don't carry that' to a product pitch when the model denied availability without searching. Gated !productAuthorityModeEnabled() (line 2291) — that flag is OFF on PRD, so this is LIVE, NOT dead. The map mislabeled the gate as !llmOwnsTurnActive(); the adversary corrected it. It ignores TurnPlan's productDisplayPolicy, so on suppress workflows not caught by isPolicyOrServiceQuestion (sizing_help, compatibility-without-named-product) it can override TurnPlan.

**Guard to add:** At the top of the block (before line 2290), add: if (ctx.turnPlan?.productDisplayPolicy === 'suppress') { /* TurnPlan says do not display products this turn */ } else { ...existing denial-recovery... }. This preserves the live corrective on display=show workflows (browse, condition_recommendation) where it is ALIGNED with TurnPlan, and only blocks it on explicit suppress turns. Do NOT demote — there is no deterministic owner inside runAgenticLoop covering the no-search false-denial case on show workflows, so evidence-only would lose the corrective entirely (legacy-removal-plan.md Group 4 conditional keeper).

### `recovery-denial-search` — `chat.jsx`

**What:** Sibling of denial-recovery at lines 2286-2324: rewrites denial text to 'Actually, take a look at these...' (line 2315) when the model denied availability without calling search_products. Also gated !productAuthorityModeEnabled() (line 2291) → LIVE on PRD. It is the UNIQUE path that searches the catalog after a hallucinated denial for refused/non-answer product turns; the downstream denial-strippers only fire when cards already exist, so on an empty-pool hallucinated denial they do nothing.

**Guard to add:** Same productDisplayPolicy==='suppress' early-skip as denial-recovery (these are the same code region, lines 2286-2324). Add: if (ctx.turnPlan?.productDisplayPolicy === 'suppress') skip the force-search + text rewrite. Keep it live on show workflows.

### `recovery-condition-search` — `chat.jsx`

**What:** Lines 2246-2284: when the model pitches condition/occasion products ('something dressy for the office') but never searched, it runs its own search AND rewrites fullResponseText (lines 2273-2275). Gated !productAuthorityModeEnabled() (line 2247) → LIVE on PRD. TurnPlan's ensureProductTurnCards re-seeds the POOL deterministically, but the recovery hop also mutates TEXT outside grounding-validator/response-contract — so it is NOT behavior-neutral and a bare demote is wrong.

**Guard to add:** Add a productDisplayPolicy==='suppress' early-skip (before line 2246). Separately, since pool-seeding IS superseded by ensureProductTurnCards, scope this path to TEXT-only recovery: guard the search/pool mutation behind a check that no TurnPlan owner will seed cards, OR add a comment marking the text rewrite as the only residual behavior to remove once a test proves grounding-validator covers the no-search pitch. Do not delete the text rewrite without that test.

### `compatibility-truth-owner` — `chat.jsx`

**What:** Deterministic orthotic<->sandal compatibility answer (lines 3135-3147) that fires WORKFLOW-AGNOSTIC on latestUserMessage pattern (comment line 3136: 'PRODUCT-TRUTH GUARD (workflow-agnostic)'). It does NOT check ctx.turnPlan.workflow, so it can own the answer on a browse/clarification/named_product_advisory turn that TurnPlan classified differently with clarificationAllowed=true.

**Guard to add:** This is a legitimate Aetrex-safety owner, so guard rather than remove. Add an explicit allow-list check: only let it own when ctx.turnPlan?.workflow is in {compatibility, named_product_advisory, product_focus, browse} OR when no clarification is pending — and log [turn-invariant] answerOwner='compatibility-truth' with the workflow it pre-empted, so any genuine conflict with a clarification turn is observable in PRD before tightening further.

### `support-handoff-gate` — `chat.jsx`

**What:** detectSupportHandoffNeed (lines 3558-3601) can own the answer in 'hard' mode (sets fullResponseText + pool=[] + support CTA, lines 3578-3589) driven by message pattern ('my order', 'when will it arrive', 'change my address'), REGARDLESS of ctx.turnPlan.workflow. Can fire on a product/recommendation turn TurnPlan owns.

**Guard to add:** Add: only allow hard-mode takeover when ctx.turnPlan?.workflow === 'customer_service' OR 'policy_account'; on any product/answer workflow, downgrade to 'soft' mode (append support text + CTA without wiping the model's product answer or the pool). This keeps the safety net while ceding answer ownership to TurnPlan on product turns.

### `finalize-pipeline-entry` — `emit-finalize.server.js`

**What:** finalizeOutboundReply (lines 555-1393) is the universal final emit that runs on BOTH paths and hosts many code-owned text/card fallbacks. It reads planForcesProductDisplay (line 1219) for the suppressors but the orchestrator itself does not assert TurnPlan ownership of the workflow before running buildAnswerWorkflowExhaustionText / buildCodeOwnedProductListingText etc.

**Guard to add:** No behavioral change to the individual guards yet — instead add a single per-call diagnostic at entry: log workflow=ctx.turnPlan?.workflow, productDisplayPolicy, clarificationAllowed, and which sub-guard (if any) mutated text/pool. This turns the pipeline into an observable surface so the PRD coverage window (preSteps) can prove which sub-guards actually fire and on which workflows before any of them is tightened or removed.

### `false-category-denial-guard` — `emit-finalize.server.js`

**What:** Lines 743-770: when the model claims the store doesn't carry a category that IS in the synced catalog, it rewrites the reply; when pool>0 and isAnswerWorkflow it calls buildAnswerWorkflowExhaustionText (line 764) which CAN override the LLM answer on an answer workflow.

**Guard to add:** It already branches on isAnswerWorkflow; tighten so the buildAnswerWorkflowExhaustionText override only fires when ctx.turnPlan owns the answer workflow (availability / comparison / condition_recommendation / multi_recommendation) AND the model text actually contains a false denial (detectFalseCategoryDenial already gates this). Add a [turn-invariant] log when it overrides, so any over-trigger is visible before further change.

### `yes-no-suppressor` — `emit-finalize.server.js`

**What:** Lines 1221-1229: suppresses the card pool when a yes/no question got a yes/no answer. Already short-circuits on planRequiresDisplay (line 1219), but can still suppress on workflows where planRequiresDisplay is false yet TurnPlan intended cards.

**Guard to add:** Already partially guarded by planRequiresDisplay. Add the inverse safety: skip suppression when ctx.turnPlan?.productDisplayPolicy === 'show' || 'show_availability' || 'show_focused' even if planForcesProductDisplay returned false for an edge case, and log when it zeroes a non-empty pool. Low urgency — this is a hardening, not a live-bug fix.

### `capability-check-suppressor` — `emit-finalize.server.js`

**What:** Lines 1235-1248: drops the pool when the customer asks a capability check about prior products without naming a new category. Guards on planRequiresDisplay but can suppress on other workflows.

**Guard to add:** Same pattern as yes-no-suppressor: add an explicit ctx.turnPlan productDisplayPolicy show-family bypass and log pool wipes. Bundle this guard with yes-no-suppressor and policy-question-suppressor as one 'suppressor hardening' change.

### `policy-question-suppressor` — `emit-finalize.server.js`

**What:** Lines 1250-1264: drops the pool when the customer asks a pure policy/service question without a product noun. Guards on planRequiresDisplay but is message-pattern driven and can suppress when TurnPlan wanted cards.

**Guard to add:** Add the show-family productDisplayPolicy bypass and log pool wipes, same as the other two suppressors. These three (yes-no, capability-check, policy-question) share the exact same shape and should be guarded together.

### `final-empty-text-guard` — `emit-finalize.server.js`

**What:** Lines 1358-1383: last-resort buildCodeOwnedProductListingText when pool>0 and text is empty after all prior repairs. CAN produce code-owned listing text overriding what should be the LLM's answer; legitimate as exhaustion text on answer workflows but a blunt fallback on browse.

**Guard to add:** Keep as the empty-text safety net (it only fires when text is already ''), but branch the wording: when isAnswerWorkflow use buildAnswerWorkflowExhaustionText, otherwise the neutral listing. Add a [turn-invariant] log answerOwner='code-owned-listing' so the (rare) firings are visible. Do not remove — the adversary confirmed removing it reintroduces the textLen=0 poolSize>0 bug.

### `clarification-card-wipe-guard` — `chat.jsx`

**What:** Lines 3495-3525: wipes cards on a pure clarifier turn. Already checks productDisplayPolicy/workflow and hasProductEvidence, but is message-pattern driven (looksLikeClarifyingQuestion) and can suppress cards a deterministic owner pinned.

**Guard to add:** It already checks planPinnedProduct and hasProductEvidence; add an explicit short-circuit when any pin owner set availabilityPinnedCards/comparisonPinnedCards/evidencePinnedCards/priorEvidencePinnedCards (currently only availabilityPinnedCards is checked via planPinnedProduct) so a pinned carousel is never wiped by the clarifier heuristic. Log when it wipes a non-empty pool.

## DEMOTE — 0

**None.** DEMOTE is structurally impossible for the empty-pool guards — the deterministic owners (availability-truth, prior-evidence, compatibility-truth) require non-empty evidence to fire, so there is nothing to demote into.

## KEEP — 33 ids

`product-turn-engine-scope-and-selection`, `runProductTurn / runProductTurnDispatch (legacy dispatcher cascade — flag-gated rollback)`, `product-turn-engine-dispatcher`, `variant-fact-dispatcher`, `policy-engine-dispatcher`, `resolver-no-match-dispatcher`, `auto-broaden`, `empty-pool-pitch-repair`, `policy-question-pure-fallback`, `repeated-clarifier-escape`, `definitional-hallucination`, `false-gender-affirmation`, `repairProductResponseText`, `buildCodeOwnedProductListing`, `repairProductTurnAssembly`, `claim-verifier-assembly`, `policy-fallback-only`, `hard-cap-length`, `comparison-use-prior-cards`, `definition-question-guard`, `broad-gender-request-runtime-pin`, `availability-truth-owner`, `prior-evidence-owner`, `evidence-plan-owner`, `evidence-plan-comparison`, `evidence-plan-multi-recommendation`, `broad-gender-browse-owner`, `condition-recommendation-pinner`, `answer-workflow-exhaustion`, `validator-blocking-enforcement`, `grounding-retry-loop`, `session-memory-build / resolveTurnIntent / applyStaleDrops (memory infra)`, `turn-plan ownership overrides in orthotic-flow-gate (KEEP cluster: condition_recommendation, product-owned-workflows, clarify-disallowed)`

## Pre-steps (do these BEFORE any cleanup — this is what makes it 100% safe)

- STEP 0 (gate the whole effort): Run `node scripts/audit-legacy-owners.mjs` and confirm it passes with no drift. This script is the source of truth for reachability; if it has drifted from the code, fix that before touching anything else. Do not proceed if it fails.

- STEP 1 (prove the kill switch is not live): Grep every PRD/staging env source (Railway dashboard vars, shopify.app*.toml, Dockerfile, .env*) to confirm NO environment sets LLM_OWNS_ALL_TURNS=false and NONE sets PRODUCT_TURN_ENGINE_ENABLED=true. This is the precondition the repo's own pre-removal checklist (legacy-removal-plan.md lines 82-88) requires. If either is set anywhere, the 'dead on PRD' classification is FALSE for that env and all KEEP-as-rollback items become live.

- STEP 2 (per-path coverage logging — the core de-risking move): Add a single one-line console.log at the ENTRY of every legacy owner, BEFORE its body, emitting a stable tag, e.g. console.log(`[legacy-cov] path=denial-recovery workflow=${ctx.turnPlan?.workflow} displayPolicy=${ctx.turnPlan?.productDisplayPolicy} flag_llmowns=${llmOwnsTurnActive()} flag_authority=${productAuthorityModeEnabled()}`). Instrument: the 4 dispatchers (chat.jsx 1160/1298/1351/613), the 3 recovery hops (2246/2286/2367 region), auto-broaden (2326), definitional-hallucination (732), false-gender-affirmation (1109), each emit-finalize suppressor (1221/1235/1250), final-empty-text-guard (1358), support-handoff hard mode (3578), compatibility-truth (3135). Static reachability is NOT 100% proof for a live system — this log IS the proof.

- STEP 3 (soak in PRD): Run the coverage logging in PRD for >=2 weeks (matches the documented clean-QA window). Aggregate the [legacy-cov] lines. A path that NEVER logs across the window on the live flag config is empirically dead and becomes a real REMOVE candidate; a path that logs is live and must stay guarded. Pair this with the existing requirement of >=2 weeks of [turn-invariant] logs with zero VIOLATION lines.

- STEP 4 (prove deterministic coverage before any future REMOVE): For each conditional keeper (denial-recovery, recovery-condition-search, recovery-denial-search), write a targeted eval that drives the exact firing case (model denies/pitches WITHOUT searching) through the LLM-owns path and asserts the grounding-validator + answer-workflow forced-search produce the same corrected outcome. legacy-removal-plan.md lines 74-78 explicitly states neither is PROVABLY a superset yet — only a green test moves them from KEEP to REMOVE.

- STEP 5 (capture a behavioral baseline): Before adding any GUARD, snapshot current outputs of eval-live-core-flows.mjs, eval-availability-truth.mjs, eval-grounding-validator.mjs, eval-turn-plan*.mjs, eval-response-contract.mjs, eval-gender-lock.mjs (the last two pin the flags OFF and exercise the rollback paths). Re-run after each guard change and diff — a guard that is truly 'only fires when TurnPlan delegates' must not change any green eval.

## Recommended sequence

1. Run preSteps 0-1 (audit script green + confirm no env flips the kill switch / authority engine). This is the foundation; everything downstream assumes the documented flag defaults hold.

2. Land preStep 2 (per-path [legacy-cov] entry logging) as a single no-behavior-change observability PR. Lowest possible risk — pure logging, zero control-flow change. Capture the eval baseline (preStep 5) in the same PR.

3. Start the 2-week PRD soak (preSteps 3) collecting [legacy-cov] + [turn-invariant] data. Do NOT delete anything during the soak.

4. While soaking, land the LIVE-path GUARDs that fix real productDisplayPolicy conflicts, in risk order, each its own small PR with eval diff: (a) the three suppressors yes-no/capability-check/policy-question (already partially guarded by planRequiresDisplay — lowest risk hardening); (b) false-category-denial-guard + final-empty-text-guard wording branch; (c) the recovery hops denial-recovery/recovery-denial-search/recovery-condition-search productDisplayPolicy==='suppress' early-skip (these are LIVE on PRD — higher blast radius, guard carefully and watch [turn-invariant]); (d) support-handoff-gate hard->soft downgrade on product workflows; (e) compatibility-truth-owner workflow allow-list; (f) clarification-card-wipe-guard pin-aware short-circuit.

5. After GUARDs land and bake, write the coverage-proof evals (preStep 4) for the three conditional-keeper recovery paths. Only paths whose eval proves grounding-validator subsumes them AND that logged zero [legacy-cov] hits become REMOVE candidates.

6. Only after the soak closes clean (zero VIOLATIONs, all evals green, no [legacy-cov] hits on the targeted paths) and the kill switch is retired (isLlmOwnsTurnEnabled() hard-coded true): delete the whole legacy dispatcher cascade (Group 1: runProductTurnDispatch/runVariantFactDispatch/runPolicyTurnDispatch/runResolverNoMatchDispatch) + Group 2 flag-gated post-processors (auto-broaden, repeated-clarifier-escape, empty-pool-pitch-repair, policy-question-pure-fallback) together in ONE change. Never piecemeal a flag-gated path while the flag still exists.

7. Separately and last, remove the PRODUCT_AUTHORITY (PRODUCT_TURN_ENGINE_ENABLED) gates and shadow-mode diagnostics (Group 3) in their own scoped change, since they thread through several guards independently of the kill switch.

## Risks

- **GATE-MISLABEL RISK (the big one): The FULL MAP repeatedly mislabels the recovery paths as gated !llmOwnsTurnActive() (dead on PRD); the ACTUAL gate at chat.jsx:2247/2291 is !productAuthorityModeEnabled(), a DIFFERENT flag that is OFF on PRD, so denial-recovery / recovery-condition-search / recovery-denial-search are LIVE on the production path right now. Any plan that trusts the map's gate labels and deletes these as 'dormant' would remove live correctives. Verified directly in code.**

- **NOTHING IS REMOVE-SAFE TODAY: Per your own rule (safeToRemove requires REMOVE-upheld + turnPlanCovers=true), zero of the 19 adversarial verdicts qualify — all were refuted to KEEP or GUARD. Treating any of them as deletable now violates the 100%-safe / no-mass-deletion constraint.**

- **KILL-SWITCH COUPLING: The four legacy dispatchers + Group-2 post-processors are dead ONLY because LLM_OWNS_ALL_TURNS defaults ON and short-circuits at chat.jsx:5898. If anyone flips the kill switch (the documented rollback), every KEEP-as-rollback item becomes live. Deleting them before the flag is retired silently breaks rollback. evals eval-response-contract.mjs / eval-gender-lock.mjs deliberately pin the flag OFF and exercise these paths — deleting the paths would red those evals.**

- **EMPTY-POOL OWNERS HAVE NOTHING TO FEED: All TurnPlan deterministic owners (availability-truth, prior-evidence, compatibility-truth) require NON-EMPTY evidence to fire. The guards that act on pool.length===0 (definitional-hallucination, false-gender-affirmation, empty-pool repairs) therefore have no deterministic owner to demote into — DEMOTE is structurally impossible for them; they are KEEP/GUARD only.**

- **TEXT-MUTATION-OUTSIDE-VALIDATOR: Several recovery paths mutate fullResponseText (chat.jsx 2273-2275, 2315) outside grounding-validator/response-contract. A naive 'demote to evidence-only' that keeps pool-seeding but drops the text rewrite is NOT behavior-neutral — the text mutation has no replacement on the legacy path. Guard, don't demote.**

- **GUARD OVER-TIGHTENING: Adding a productDisplayPolicy==='suppress' skip to the recovery hops could, if productDisplayPolicy is ever unset/null for a workflow that should suppress, fail open. Mitigate by treating only the explicit show-family ('show','show_availability','show_focused') as 'display intended' and everything else as neutral, plus the [turn-invariant] log to catch regressions during the soak.**

- **compatibility-truth WORKFLOW-AGNOSTIC BY DESIGN: It intentionally fires on message pattern for Aetrex orthotic<->sandal safety. An allow-list guard that is too narrow could let an unsafe orthotic-in-sandal affirmation slip through on a browse turn. The guard must FAIL SAFE toward running the truth answer, not toward suppressing it.**

## Full mapped-path inventory (180)

| id | file | owns | readsTurnPlan | conflict | class |
|---|---|---|---|---|---|

| resolver-constraint-propagation-core | catalog-resolver.server.js | none-infra | ignores | none | KEEP |

| resolver-no-match-dispatch | chat.jsx | text+cards | ignores | high | GUARD |

| resolver-no-match-text-builder | response-contract.server.js | text | ignores | medium | GUARD |

| resolver-promised-recommendation-check | chat-postprocessing.js | none-infra | ignores | none | KEEP |

| product-turn-dispatch-main | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| product-turn-engine-scope-and-selection | product-turn-engine.server.js | none-infra | reads-but-can-override | high | DEMOTE |

| engine-wants-this-turn-gate | product-turn-engine.server.js | routing-decision | ignores | high | GUARD |

| variant-fact-dispatch | chat.jsx | text+cards | ignores | medium | GUARD |

| policy-turn-dispatch | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| resolver-has-exact-no-match-predicate | chat.jsx | none-infra | ignores | none | KEEP |

| catalog-resolver-final-dispatch | catalog-resolver.server.js | none-infra | ignores | none | KEEP |

| product-turn-engine-similar-path | product-turn-engine.server.js | text+cards | ignores | high | GUARD |

| detect-similar-product-intent | product-turn-engine.server.js | routing-decision | ignores | medium | GUARD |

| build-alternative-category-choices | product-turn-engine.server.js | none-infra | ignores | none | KEEP |

| resolver-extract-constraints-from-message | catalog-resolver.server.js | none-infra | ignores | none | KEEP |

| router-main-dispatch-sequence | chat.jsx | routing-decision | reads-but-can-override | high | GUARD |

| shoes-vs-orthotics-decision | orthotic-flow-gate.server.js | text+cards | ignores | high | GUARD |

| turn-plan-condition-recommendation-override | orthotic-flow-gate.server.js | none-infra | reads-but-can-override | none | KEEP |

| turn-plan-product-owned-workflows | orthotic-flow-gate.server.js | none-infra | reads-but-can-override | none | KEEP |

| turn-plan-clarify-disallowed | orthotic-flow-gate.server.js | none-infra | reads-but-can-override | none | KEEP |

| llm-owned-mode-defer | orthotic-flow-gate.server.js | none-infra | ignores | none | KEEP |

| resolver-strong-action | orthotic-flow-gate.server.js | none-infra | ignores | none | KEEP |

| short-ambiguous-reply-guard | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| comparison-value-question | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| efficacy-skeptic-question | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| footwear-inclusive-request | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| product-info-followup | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| goal-guard-info-question | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| unified-gate-accumulation | orthotic-flow-gate.server.js | routing-decision | ignores | none | KEEP |

| subject-pivot-reset | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| fresh-arch-reset | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| customer-correction-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| subject-clarification-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| meta-frustration-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| give-up-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| confusion-repair | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| frustration-repair | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| gender-restatement-repair | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| meta-question-source-challenge | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| domain-disambig | orthotic-flow-gate.server.js | text+cards | ignores | medium | KEEP |

| exact-chip-answer-override | orthotic-flow-gate.server.js | none-infra | ignores | none | KEEP |

| usecase-vocabulary-guard | orthotic-flow-gate.server.js | none-infra | ignores | none | KEEP |

| path-ambiguity-disambig | orthotic-flow-gate.server.js | text+cards | ignores | medium | KEEP |

| kids-auto-fill | orthotic-flow-gate.server.js | none-infra | ignores | none | KEEP |

| orthotic-rejection-hard-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| off-topic-side-question-mid-flow | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| availability-question-before-footwear-gate | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| footwear-hard-veto-1 | orthotic-flow-gate.server.js | text+cards | ignores | medium | KEEP |

| non-orthotic-footwear-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| footwear-commit-history-veto | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| engagement-rule-intent-or-fingerprint | orthotic-flow-gate.server.js | routing-decision | ignores | none | KEEP |

| off-topic-reply-on-fingerprint | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| layer-3-constrained-haiku-call | orthotic-flow-gate.server.js | routing-decision | ignores | none | KEEP |

| unmappable-reply-fallthrough | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| mid-flow-question-doesnt-answer-chip | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| educational-clinical-question | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| informational-question-mid-flow | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| availability-question-mid-flow | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| next-question-selection | orthotic-flow-gate.server.js | routing-decision | ignores | none | KEEP |

| gender-stall-guard | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| hard-loop-cap | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| question-emit | orthotic-flow-gate.server.js | text+cards | ignores | none | KEEP |

| resolve-intent-guard | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| resolve-execution | orthotic-flow-gate.server.js | text+cards | ignores | none | KEEP |

| resolve-emit | orthotic-flow-gate.server.js | text+cards | ignores | none | KEEP |

| resolve-fallthrough | orthotic-flow-gate.server.js | none-infra | ignores | low | KEEP |

| recommender-required-attrs-gate | recommender-tools.server.js | clarifier | guarded | high | GUARD |

| recommender-sandal-incompatibility-guard | recommender-tools.server.js | text | ignores | low | KEEP |

| recommender-derivations | recommender-tools.server.js | none-infra | ignores | none | KEEP |

| recommender-clinical-enrichment | recommender-tools.server.js | none-infra | ignores | none | KEEP |

| recommender-resolver-call | recommender-tools.server.js | none-infra | ignores | none | KEEP |

| cheat-code-dump | chat.jsx | text+cards | ignores | none | KEEP |

| variant-fact-dispatcher | chat.jsx | text+cards | ignores | low | KEEP |

| policy-engine-dispatcher | chat.jsx | text+cards | ignores | low | KEEP |

| resolver-no-match-dispatcher | chat.jsx | text+cards | ignores | low | KEEP |

| soft-browse-refine | chat.jsx | text+cards | ignores | medium | GUARD |

| orthotic-gate-escape-soft-browse | chat.jsx | text+cards | reads-but-can-override | low | KEEP |

| denial-recovery | chat.jsx | text+cards | ignores | high | DEMOTE |

| auto-broaden | chat.jsx | cards | ignores | medium | DEMOTE |

| repeat-clarifier-guard | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| broad-gender-browse | chat.jsx | text+cards | reads-but-can-override | low | KEEP |

| condition-recommendation-pinner | chat.jsx | cards | reads-but-can-override | none | KEEP |

| rewrite-only-restoration | chat.jsx | cards | ignores | none | KEEP |

| exact-no-match-fallback | chat.jsx | text | reads-but-can-override | medium | GUARD |

| non-shoppable-guard | chat.jsx | cards | ignores | none | KEEP |

| wrong-topic-guard | chat.jsx | cards | ignores | low | KEEP |

| category-gender-rewrite | chat.jsx | routing-decision | reads-but-can-override | high | GUARD |

| broad-gender-request-detector | chat.jsx | text+cards | reads-but-can-override | low | KEEP |

| product-turn-engine-dispatcher | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| suppress-cards-for-chips | chat.jsx | cards | reads-but-can-override | high | GUARD |

| prior-evidence-availability-owner | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| evidence-plan-owner | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| session-memory-build | session-memory.server.js | memory-rewrite | ignores | high | KEEP |

| apply-stale-drops | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| resolve-turn-intent | turn-intent.server.js | routing-decision | ignores | none | KEEP |

| detect-recipient | session-memory.server.js | text | ignores | low | KEEP |

| stale-all-subject-bound | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| session-gender-early-pivot | chat.jsx | text | ignores | low | KEEP |

| gender-authority-from-classifier | chat.jsx | text | ignores | medium | GUARD |

| memory-gender-authority-restore | chat.jsx | memory-rewrite | ignores | none | KEEP |

| resolved-family-gender-override | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| early-session-memory-build | chat.jsx | none-infra | ignores | none | KEEP |

| broad-gender-request-runtime-pin | chat.jsx | text+cards | ignores | medium | DEMOTE |

| compare-turn-check | chat.jsx | none-infra | ignores | none | KEEP |

| catalog-contradiction-resolution | session-memory.server.js | memory-rewrite | ignores | low | KEEP |

| classifier-attributes-layering | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| resolver-inferred-layering | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| extracted-scalar-application | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| choice-events-layering | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| normalizeMerchandisingScope | session-memory.server.js | memory-rewrite | ignores | none | KEEP |

| soft-gender-gate-browse | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| orthotic-gate | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| variant-fact-dispatch | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| policy-engine-dispatch | chat.jsx | text+cards | reads-but-can-override | medium | GUARD |

| resolver-no-match-dispatch | chat.jsx | text+cards | reads-but-can-override | high | GUARD |

| product-turn-engine-dispatch | chat.jsx | text+cards | reads-but-can-override | medium | GUARD |

| agent-loop-fallback | chat.jsx | text+cards | reads-but-can-override | low | KEEP |

| finalize-pipeline-entry | emit-finalize.server.js | text+cards | reads-but-can-override | high | GUARD |

| cleanup-pipeline | emit-finalize.server.js | text | ignores | low | KEEP |

| hard-cap-length | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| empty-pool-pitch-repair | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| definitional-hallucination | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| false-category-denial-guard | emit-finalize.server.js | text+cards | reads-but-can-override | high | GUARD |

| empty-text-resolver-promise | emit-finalize.server.js | text | ignores | low | KEEP |

| empty-text-repair-fallback | emit-finalize.server.js | text | reads-but-can-override | medium | GUARD |

| policy-question-pure-fallback | emit-finalize.server.js | text+cards | ignores | medium | DEMOTE |

| compound-policy-product-clause | emit-finalize.server.js | text | ignores | low | KEEP |

| gender-chip-strip-unsupported | emit-finalize.server.js | cards | ignores | low | KEEP |

| gender-chip-filter-contradicting | emit-finalize.server.js | cards | ignores | low | KEEP |

| gender-chip-suppress-plan | emit-finalize.server.js | cards | guarded | none | KEEP |

| gender-chip-decorate | emit-finalize.server.js | cards | ignores | low | KEEP |

| catalog-scoped-navigation-chips | emit-finalize.server.js | cards | ignores | low | KEEP |

| yes-no-suppressor | emit-finalize.server.js | cards | reads-but-can-override | medium | GUARD |

| capability-check-suppressor | emit-finalize.server.js | cards | reads-but-can-override | medium | GUARD |

| policy-question-suppressor | emit-finalize.server.js | cards | reads-but-can-override | medium | GUARD |

| claim-verifier-assembly | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| unsafe-inline-chips-strip | emit-finalize.server.js | cards | ignores | low | KEEP |

| ensure-complete-customer-text-finalize | emit-finalize.server.js | text | ignores | low | KEEP |

| final-empty-text-guard | emit-finalize.server.js | text | reads-but-can-override | high | GUARD |

| compound-policy-final-guard | emit-finalize.server.js | text | ignores | low | KEEP |

| repairProductResponseText | response-contract.server.js | text | ignores | medium | DEMOTE |

| buildCodeOwnedProductListing | response-contract.server.js | text | ignores | high | DEMOTE |

| ensureCompleteCustomerText-response | response-contract.server.js | text | ignores | low | KEEP |

| repairProductTurnAssembly | response-contract.server.js | text | ignores | medium | DEMOTE |

| policy-fallback-only | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| compound-policy-fallback-text | emit-finalize.server.js | text | ignores | low | KEEP |

| answer-workflow-exhaustion | emit-finalize.server.js | text | guarded | none | KEEP |

| false-gender-affirmation | emit-finalize.server.js | text | ignores | medium | DEMOTE |

| stock-claim-hallucination | emit-finalize.server.js | text | ignores | low | KEEP |

| disallowed-clarifier-strip | chat-postprocessing.js | text | guarded | low | KEEP |

| validator-blocking-enforcement | grounding-validator.server.js | none-infra | reads-but-can-override | high | GUARD |

| grounding-retry-loop | llm-owns-turn.server.js | none-infra | reads-but-can-override | medium | KEEP |

| availability-truth-owner | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| prior-evidence-owner | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| compatibility-truth-owner | chat.jsx | text | ignores | medium | GUARD |

| evidence-plan-comparison | chat.jsx | cards | reads-but-can-override | none | KEEP |

| evidence-plan-multi-recommendation | chat.jsx | cards | reads-but-can-override | none | KEEP |

| evidence-plan-compatibility-named | chat.jsx | cards | reads-but-can-override | low | KEEP |

| evidence-plan-named-product-advisory | chat.jsx | cards | reads-but-can-override | none | KEEP |

| evidence-plan-product-focus | chat.jsx | cards | reads-but-can-override | none | KEEP |

| evidence-plan-display-recovery | chat.jsx | cards | reads-but-can-override | none | KEEP |

| broad-gender-browse-owner | chat.jsx | text+cards | reads-but-can-override | medium | KEEP |

| condition-recommendation-evidence-select | chat.jsx | cards | reads-but-can-override | none | KEEP |

| clarifier-gate-repair | chat.jsx | text | reads-but-can-override | low | GUARD |

| clarifier-strip-disallowed | chat.jsx | text | reads-but-can-override | low | GUARD |

| clarification-card-wipe-guard | chat.jsx | cards | reads-but-can-override | medium | GUARD |

| customer-service-handoff | chat.jsx | text+cards | reads-but-can-override | none | KEEP |

| support-handoff-gate | chat.jsx | text+cards | ignores | high | GUARD |

| recovery-condition-search | chat.jsx | text | ignores | high | DEMOTE |

| recovery-denial-search | chat.jsx | text | ignores | high | DEMOTE |

| repeated-clarifier-escape | chat.jsx | text | ignores | high | DEMOTE |

| exact-no-match-code-owned | chat.jsx | text | ignores | low | KEEP |

| pool-filter-non-shoppable | chat.jsx | cards | ignores | none | KEEP |

| pool-filter-wrong-topic | chat.jsx | cards | ignores | none | KEEP |

| pool-filter-rejected-category | chat.jsx | cards | ignores | none | KEEP |

| comparison-use-prior-cards | chat.jsx | cards | ignores | medium | DEMOTE |

| named-product-mismatch-guard | chat.jsx | cards | ignores | medium | GUARD |

| definition-question-guard | chat.jsx | cards | ignores | medium | DEMOTE |

| rewrite-only-restoration | chat.jsx | cards | ignores | none | KEEP |

| text-cleanup-before-emit | chat.jsx | text | ignores | low | KEEP |
